export enum TAB_STATUS_ENUM {
    COMPLETE = 'complete'
}

export enum ENUM_RESUME {
    PREVIEW = 'resume::preview'
}

export enum ENUM_PLATFORM {
    BOSS = 1,
    LIEPIN = 2,
}

export enum VisibilityStateEnum {
    HIDDEN = 'hidden',
    VISIBLE = 'visible'
}

export enum SendVerifyCodeTypeEnum {
    LOGIN = 1,
    REGISTER = 2
}

export enum ResumeStatusEnum {
    CHECKING = 0,
    SUITABLE = 1,
    UNSUITABLE = 2

}

export enum HiringChatMessageSenderTypeEnum {
    SYSTEM = 1,
    CANDIDATE = 2,
    HIRING = 3
}

export enum HiringChatMessageTypeEnum {
    TEXT = 1,
    IMAGE = 2,
    FILE = 3,
    WECHAT = 4,
    RESUME = 5,
}

export enum ResumeStatus {
    CHECKING = 0,
    SUITABLE = 1,
    UNSUITABLE = 2,
    ERROR = 3,
}
